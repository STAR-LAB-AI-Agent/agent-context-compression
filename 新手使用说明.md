# 新手使用说明

你不需要先理解 AI Agent。把这个项目暂时理解为一个“长对话整理器”：输入一份长对话文本，程序会输出更短、分好类的 JSON，并告诉你节省了多少 Token。

## 第一次使用

1. 确认电脑安装了 Python 3.10～3.12。在“开始”菜单搜索 `cmd`，输入 `python --version`。能看到版本号即可。
2. 双击项目目录中的 `一键安装.bat`，等待出现“安装成功”。这一步需要联网，通常只执行一次。
3. 双击 `一键验证.bat`。
4. 如果窗口出现 `9 passed` 和“验证成功”，说明代码功能、异常处理和安全检查都通过。
5. 记事本会自动打开 `outputs\my_result.json`，这是压缩结果。

## 怎样判断结果是正确的

打开结果后重点看：

- `summary`：旧对话的简要内容；
- `decisions`：已经决定的事项；
- `todos`：下一步要做什么；
- `constraints`：必须遵守或禁止的事项；
- `open_questions`：尚未解决的问题；
- `recent_messages`：最近两条消息，应与原文完全一致；
- `metrics.tokens_before`：压缩前 Token；
- `metrics.tokens_after`：压缩后 Token；
- `metrics.saving_rate`：节省比例，正数表示确实减少了 Token；
- `metrics.keyword_recall_at_20`：重要关键词保留率，越接近 1 越好。

示例预期约为：压缩前 950 Token、压缩后 696 Token、节省率 0.2674。不同 tiktoken 小版本可能略有差别。

## 使用自己的对话

复制 `examples\sample_chat.txt`，改名为 `my_chat.txt`，然后按以下形式填写：

```text
用户：我们下周要交项目，必须支持 JSON 输出。
助手：决定使用 Python 开发，并保留最近两条消息。
用户：下一步需要完成测试。是否能统计 Token？
```

在项目文件夹地址栏输入 `cmd` 并回车，然后运行：

```bat
.venv\Scripts\python.exe -m context_compactor my_chat.txt -i "请压缩并对比 Token" --root . --pretty -o outputs\my_chat_result.json
```

之后打开 `outputs\my_chat_result.json`。如果原文很短，结构化 JSON 的字段开销可能使压缩后 Token 更多，这是正常现象；这个工具主要面向长对话。

## 答辩时最简单的演示

1. 展示 `examples\sample_chat.txt`，说明这是待压缩的长对话。
2. 双击 `一键验证.bat`。
3. 指出 `9 passed`，证明 9 个正常、边界与异常测试全部通过。
4. 在自动打开的 JSON 中指出摘要、决定、待办、约束和最近消息。
5. 指出 `tokens_before > tokens_after`，并说明 `saving_rate` 是 Token 节省率。

一句话解释 AI Agent：它先理解用户说的是“普通压缩、主题压缩还是效果评估”，再自动选择压缩工具并返回结构化结果。
