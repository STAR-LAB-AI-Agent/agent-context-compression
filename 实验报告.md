# 《智能体开发实战》课程实验报告

## 一、项目基本信息

- 选题：17 - AI Agent 上下文压缩
- 项目名称：Context Compactor
- 开发语言：Python 3.10+
- 交互形态：自然语言意图路由 + Skill + Script/CLI
- 作者/学号：________（提交前填写）

## 二、需求分析

### 2.1 背景

智能体执行长任务时，对话历史会不断增长。若每次调用都携带全部历史，会增加 Token 成本与延迟，也会让模型更难定位当前约束。项目目标是先在本地完成筛选、分类和压缩，仅把必要信息送入后续模型上下文。

### 2.2 目标用户

需要运行长对话、代码协作、资料整理或多步骤任务的智能体开发者，以及希望观察 Token 优化效果的课程学习者。

### 2.3 三类核心意图

| 意图 | 示例 | 系统行为 |
|---|---|---|
| 通用结构化压缩 | “压缩这段对话，保留最近两条” | 提取摘要与五类记忆，原样保留近期消息 |
| 主题聚焦压缩 | “围绕安全约束保留信息” | 提升主题词相关句子的评分 |
| 质量与 Token 评估 | “比较压缩前后 Token 和质量” | 输出 Token、节省率、关键词保留率 |

### 2.4 功能与非功能需求

基础功能包括 TXT/Markdown/JSON 输入、角色解析、结构化压缩、参数控制与 JSON 输出。非功能需求包括离线可运行、可测试、低 Token、目录最小权限、无敏感正文日志和友好错误提示。题目推荐的可选功能“压缩前后质量与 Token 对比”也已完成。

## 三、总体架构

系统采用分层结构：

1. `agent.py`：把自然语言路由为通用、聚焦、评估三类意图。
2. `SKILL.md`：描述使用时机、参数、调用步骤、输出和示例。
3. `cli.py`：独立命令行入口，负责参数、日志和结果输出。
4. `parser.py` 与 `security.py`：输入解析和最小权限检查。
5. `compressor.py`：句子评分、结构化抽取、预算控制和质量统计。
6. `tokenizer.py`：集成 jieba 与 tiktoken，并提供离线降级。

完整链路是：“用户自然语言 → 意图路由 → Skill 参数约定 → Python CLI → jieba/tiktoken → 结构化结果”。

## 四、关键实现

### 4.1 结构化记忆

输出不是单段摘要，而是 `summary`、`facts`、`decisions`、`todos`、`constraints`、`open_questions`、`recent_messages`。结构化格式便于下游智能体只取所需字段，也降低重复解释成本。

### 4.2 抽取式排序

每个句子的分数由词频重要性、用户主题匹配、时间位置、任务关键词和角色权重构成。包含“必须、决定、待办、错误”等信号的句子获得额外权重。抽取式方法不生成新事实，适合离线、低风险演示。

### 4.3 近期消息保真

最近 N 条消息不参与摘要竞争，而是原样放入 `recent_messages`，保留当前指令与即时状态。更早内容经过压缩，兼顾短期工作记忆和长期摘要。

### 4.4 开源项目集成

- jieba 0.42.1（MIT）：实际用于中文分词、句子评分与关键词统计。
- tiktoken 0.7+（MIT）：实际用于 `cl100k_base` Token 计数。
- pytest 8+（MIT）：测试框架。

若运行环境未安装 jieba/tiktoken，程序会降级为正则切分和估算计数，不会中断现场演示。正式数据应安装完整依赖并在结果中核对 `tokenizer` 字段。

## 五、低 Token 与性能优化

优化发生在模型调用之前：输入由本地 Python 完成切分、排序、分类和预算裁剪，模型只接收压缩后的 JSON。算法主要为一次词频统计和一次排序，时间复杂度约为 O(W + S log S)，W 为词数、S 为句子数。

可验证指标如下：

- 压缩前/后 Token 数和 Token 节省量；
- 压缩比（after / before）与节省率；
- Top-20 关键词保留率，作为低成本质量代理；
- 不记录全文的结构化运行日志。

`--ratio` 控制摘要比例，`--max-tokens` 提供软预算，`--recent` 控制近期消息数量。该设计避免把全文无条件送入模型，满足任务书的低 Token 要求。

## 六、安全与异常处理

1. 通过 `--root` 限制输入白名单目录，解析后的真实路径必须位于根目录内，阻止 `../` 越界。
2. 仅允许 `.txt`、`.md`、`.json`，输入大小限制为 5 MB。
3. 默认不联网、不调用外部模型、不需要 API Key。
4. 日志只存元数据和指标，不记录对话原文、密钥或个人数据。
5. 空输入、JSON 格式错误、非法比例、目录越界和文件不存在均返回中文错误并使用非零退出码。
6. 工具只读取输入，只有用户显式指定 `--output` 时才写结果，不包含删除或移动功能。

## 七、测试设计与结果

测试集共 9 项：

| 编号 | 类型 | 内容 | 预期 |
|---:|---|---|---|
| 1 | 异常 | 空上下文 | 拒绝并提示 |
| 2 | 边界 | 比例为 0 | 参数校验失败 |
| 3 | 正常 | 结构化对话压缩 | 类别与指标存在 |
| 4 | 正常 | 最近消息保留 | 内容逐字一致 |
| 5 | 正常 | 三类自然语言意图 | 路由正确 |
| 6 | 正常 | 中文角色标签 | 角色解析正确 |
| 7 | 正常 | JSON 消息输入 | 正确读取 |
| 8 | 安全 | 目录越界 | 拒绝访问 |
| 9 | 异常 | CSV 扩展名 | 拒绝并提示 |

复现命令：`pytest -q`。2026-09-07 的实际验证结果为：`9 passed in 0.56s`。长对话样例使用 tiktoken/cl100k_base 统计，压缩前 950 Token、压缩后 696 Token，减少 254 Token，节省率 26.74%，Top-20 关键词保留率为 1.0。示例数据是虚构内容，不含真实个人信息。

## 八、验收与演示方案

先展示通用压缩得到的结构化 JSON；再用“围绕安全约束保留信息”展示主题聚焦；最后用评估意图展示 Token 节省率和关键词保留率，并运行测试。项目提供 `demo.ps1` 与 1 分钟讲稿，便于现场复现。

## 九、局限与改进

规则分类对隐含语义的识别有限，关键词指标也不能完全代表事实一致性。后续可加入 Sentence Transformers 做语义相关性重排，增加人工标注集上的事实保留率，并把压缩结果接入真实智能体框架。当前版本优先保证基础功能稳定、可解释和离线可复现。

## 十、结论

项目完成了任务书要求的三类意图、独立 CLI、Skill 说明、真实开源项目集成、安全边界、日志、9 个测试和低 Token 优化，并完成推荐的质量与 Token 对比功能。整体方案轻量、可运行、可交互、可解释，适合课程验收。
