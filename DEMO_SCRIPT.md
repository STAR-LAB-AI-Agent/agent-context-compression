# 1 分钟演示讲稿

大家好，我的题目是“AI Agent 上下文压缩”。它解决长对话持续占用 Token、关键信息容易被淹没的问题。

首先运行通用压缩。输入是一段多轮对话，系统自动提取摘要、事实、决定、待办、约束、未决问题，并原样保留最近两条消息。输出是结构化 JSON，可以直接交给下一个智能体调用。

第二个场景是主题聚焦。我输入“围绕安全约束保留信息”，路由器识别聚焦压缩意图，相关句子获得更高权重。

第三个场景展示可选功能：系统统计压缩前后 Token、节省率和 Top-20 关键词保留率。Token 优先由 tiktoken 精确计算，未安装时自动降级估算。

安全方面，程序只读取白名单目录内的 txt、md、json 文件，限制 5 MB，日志只记录元数据，不记录对话正文。最后运行 9 个测试，覆盖正常、边界和异常情况。
